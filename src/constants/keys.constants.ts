// actual keyboard keys (used in things like tooltips and any keyboard navigation)
enum KEYBOARD_KEYS {
  ARROW_UP = 'ArrowUp',
  ARROW_DOWN = 'ArrowDown',
  ENTER = 'Enter',
  ESCAPE = 'Escape',
  SPACE = ' ',
  TAB = 'Tab',
}

export default KEYBOARD_KEYS;
